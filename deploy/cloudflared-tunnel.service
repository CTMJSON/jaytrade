[Unit]
Description=Cloudflare Quick Tunnel for JayTrade
After=network.target stock-simulator.service
Wants=stock-simulator.service

[Service]
Type=simple
ExecStart=/usr/local/bin/cloudflared tunnel --url http://localhost:3001
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
